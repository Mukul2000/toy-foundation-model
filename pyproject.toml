[project]
name = "toy-foundation-model"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "datasets>=5.0.1",
    "pytest>=9.1.1",
    "tokenizers>=0.22.2",
    "torch>=2.13.0",
    "transformers>=5.14.1",
]

[[tool.uv.index]]
url = "https://pypi.org/simple"
default = true
